import 'package:flutter/material.dart';

class OrderedDetails extends StatelessWidget {
  const OrderedDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
